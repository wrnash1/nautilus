# User Feedback Action Items

- [x] **Simplify Installer (Option B)**
  - Remove "Business Name" and "Phone" fields from the installer configuration step.
  - Keep only System Check, Database Connection, and Admin Account creation.

- [ ] **Fix Auto-Login Security Issue**
  - Verify AuthMiddleware is working correctly.
  - Ensure accessing the dashboard requires a valid login session.
  - Redirect unauthenticated users to the login page.

- [ ] **Implement Setup Wizard**
  - Create a "Complete Setup" page or modal after the first admin login.
  - Collect Company Name, Phone, and other business details here.
  - Save this information to the `system_settings` table.

- [ ] **Fix Company Name Display**
  - Ensure the application fetches the company name from `system_settings` instead of using a hardcoded default.

- [ ] **Fix Migration Warnings**
  - Investigate and fix the 40 warnings occurring during migration (specifically migration 100).
